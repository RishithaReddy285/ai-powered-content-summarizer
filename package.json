{
  "name": "ai-powered-content-summarizer",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "groq-sdk": "*",
    "langsmith": "^0.7.2"
  }
}
